{
    "name": "nisu-teacher",
    "version": "1.0.0",
    "description": "NISU Teacher application with Google authentication and database storage",
    "main": "app.js",
    "scripts": {
        "start": "nodemon app.js",
        "dev": "nodemon app.js",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "@google-cloud/datastore": "^7.5.1",
        "cors": "^2.8.5",
        "express": "^4.18.2",
        "google-auth-library": "^8.7.0",
        "dotenv": "^16.4.7"
    },
    "devDependencies": {
        "nodemon": "^2.0.22"
    },
    "engines": {
        "node": ">=16.0.0"
    }
}
